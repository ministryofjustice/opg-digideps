imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

framework:
    #esi:             ~
    translator:      { fallback: "%locale%" }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      
        enabled: true
        enable_annotations: true
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_proxies: ~
    session: 
      name: digideps
      # force session usage, no cookie (we want to logout when the browser closes)
      cookie_lifetime: 0
      gc_probability: 1 
      gc_divisor: 100
      # session timeout (when GC kicks in)
      gc_maxlifetime: %session_expire_seconds%
      # true:  cookie won't be accessible by Javascript, safer for XSS
      cookie_httponly: true
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    globals:
        env: %env%
        session_expires_seconds: %session_expire_seconds%
        session_popup_show_after: %session_popup_show_after%
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    exception_controller:  AppBundle:Exception:showException
    form:
        resources:
            - 'AppBundle:Form:fields.html.twig'
        

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"
            
#    orm:
#        auto_generate_proxy_classes: "%kernel.debug%"
#        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: smtp
    host:      localhost
    username:  ~
    password:  ~
    spool:     { type: memory }
    
parameters:
    asset_dropdown:
        1: dropdown.property 
        2: dropdown.savings_accounts
        3: dropdown.investment_bonds 
        4: dropdown.isa 
        5: dropdown.unit_trusts 
        6: dropdown.national_savings_certificates 
        7: dropdown.stocks_shares
        8: dropdown.premium_bonds
        9: dropdown.vehicles 
        10: dropdown.artwork 
        11: dropdown.antiques 
        12: dropdown.jewellery
        14: dropdown.assets_held_outside_england_and_wales
        15: dropdown.court_funds_office_accounts 
        16: dropdown.other_assets