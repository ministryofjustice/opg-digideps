{% set translationDomain = "odr-deputy-expenses" %}
{% trans_default_domain translationDomain %}

<table class="check-your-answers columns-2 push-double--bottom">
    <thead>
        <tr>
            <th>Question</th>
            <th>Answer</th>
            <th>
                <span class="visuallyhidden">Actions</span>
            </th>
        </tr>
    </thead>
    <tbody>
        <tr class="behat-region-paid-for-anything">
            <td>
                {{ 'existPage.form.paidForAnything.label' | trans(transOptions)  }}
            </td>
            <td>
                {{ odr.paidForAnything | capitalize }}
            </td>
            <td class="change-answer">
                <a href="{{ path('odr_deputy_expenses_exist', { 'odrId': odr.id, 'from': 'summary' }) }}" class="behat-link-edit">{{ 'summaryPage.edit'| trans }}</a>
            </td>
        </tr>
    </tbody>
</table>

{% if odr.expenses | length > 0 %}
    <div class="grid-row">
        <div class="column-half">
            <h2 class="heading-medium flush--ends">List of expenses</h2>
        </div>
        <div class="column-half text--right">
            <a href="{{ path('odr_deputy_expenses_add', { 'odrId': odr.id, 'from': 'summary'}) }}" class="bold-small move-quarter--up action-link behat-link-add">
                <span class="icon icon-plus"></span>
                {{ 'summaryPage.addButton' | trans }}
            </a>
        </div>
    </div>
{% endif %}

{% if odr.expenses | length > 0 %}
    <table class="check-your-answers columns-3">
        <thead>
            <tr>
                <th>Description of expense</th>
                <th class="text--right">Amount</th>
                <th>
                    <span class="visuallyhidden">Actions</span>
                </th>
            </tr>
        </thead>
        <tbody>
            {% for expense in odr.expenses %}
                <tr class="behat-region-expense-{{ expense.explanation | behat_namify }}">
                    <td class="">
                        {{ expense.explanation }}<br/>
                    </td>
                    <td class="numeric-small">
                        £{{ expense.amount | money_format }}
                    </td>
                    <td class="change-answer width-sixth soft-half--left hard--right">
                        <a href="{{ url('odr_deputy_expenses_edit', { 'odrId': odr.id, 'expenseId': expense.id }) }}" class="behat-link-edit action-link">{{ 'summaryPage.edit'| trans }}</a>
                        <a href="{{ path('odr_deputy_expenses_delete', { 'odrId': odr.id, 'expenseId': expense.id }) }}" class="push-half--left action-link behat-link-delete">Remove</a>
                    </td>
                </tr>
            {% endfor %}
            <tr class="no-border">
                <td>
                    <span class="bold-small">Total amount</span>
                </td>
                <td class="numeric-small">
                    <span class="bold-small behat-region-expense-total">£{{ odr.expensesTotalValue | money_format }}</span>
                </td>
                <td></td>
            </tr>
        </tbody>
    </table>

{% endif %}
