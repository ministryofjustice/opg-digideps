{% set translationDomain = "odr-overview" %}
{% trans_default_domain translationDomain %}

<h1 class="heading-xlarge flush--bottom behat-region-report-title">
    {{ 'heading.title' | trans }}
</h1>
<h2 class="heading-large flush--top">{{ client.fullname }}</h2>

{% if odrStatus.isReadyToSubmit %}
    <div id="header-report-submit" class="hero">
        <h2 class="heading-large flush--ends">{{ 'odrReady' | trans }}</h2>
        <a class="button push-half--top behat-link-odr-submit" href="{{ path('odr_review', {'odrId': odr.id}) }}">{{ 'continue' | trans({}, 'common') }}</a>
    </div>
{% endif %}