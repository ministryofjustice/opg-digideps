{% extends 'AppBundle:Layouts:application.html.twig' %}

{% set translationDomain = "report-display" %}
{% trans_default_domain translationDomain %}

{% set isLayDeputy = not app.user.isDeputyPa %}

{% if odr.submitted == false %}
    {% if not odrStatus.isReadyToSubmit %}
        {% set preview = true %}
        {% set page = 'ndr-preview' %}
    {% else %}
        {% set preview = false %}
        {% set page = 'ndr-review' %}
    {% endif %}
{% else %}
    {% set preview = false %}
    {% set page = 'ndr-submitted' %}
{% endif %}

{% block htmlTitle %}{{ (page ~ '.htmlTitle') | trans }}{% endblock %}
{% block pageTitle %}{{ (page ~ '.pageTitle') | trans }}{% endblock %}
{% block pageTitleClass %}heading-large{% endblock %}

{% block breadcrumbs %}
    {% if odr.submitted == true %}
        <a id="overview-button" href="{{ path('homepage') }}" class="link-back">{{ 'yourReports' | trans({}, 'common' ) }}</a>
    {% else %}
        {{ macros.breadcrumbs(odr) }}
    {% endif %}
{% endblock %}

{% block pageContent %}

    <div class="push--bottom">
        {% if odr.submitted == false %}
            <p class="text">{{ (page ~ '.introPara') | trans() }}</p>
            {% if preview %}
                <a class="button" href="{{ path("odr_overview" , {'odrId': odr.id }) }}">
                    {{ 'incomplete.backToEditingReport' | trans() }}
                </a>
                <a class="link-icon link-icon-pdf button-link" href="{{ path('odr_pdf', {'odrId': odr.id}) }}">{{ 'downloadAsPdf' | trans() }}</a>
            {% endif %}
        {% else %}
            <p class="text">{{ (page ~ '.introPara') | trans() }} {{ odr.submitDate | date("j F Y") }}.</p>
            <a class="button" href="{{ path('homepage') }}">{{ 'backToYourReports' | trans({}, 'common') }}</a>
            <a class="link-icon link-icon-pdf button-link" href="{{ path('odr_pdf', {'odrId': odr.id}) }}">{{ 'downloadAsPdf' | trans() }}</a>
        {% endif %}
    </div>

    <div class="push--bottom">
        {% include 'AppBundle:Odr/Formatted:formatted.html.twig' %}
    </div>

    {% if odr.submitted == false %}
        {% if preview %}
            <a class="button" href="{{ path("odr_overview" , {'odrId': odr.id }) }}">
                {{ 'incomplete.backToEditingReport' | trans() }}
            </a>
        {% else %}
            <a class="button behat-link-odr-declaration-page" href="{{ path("odr_declaration" , {'odrId': odr.id }) }}">{{ 'continue' | trans({}, 'common') }}</a>
            <a class="button-link" href="{{ path("odr_overview" , {'odrId': odr.id }) }}">
                {{ 'incomplete.backToEditingReport' | trans() }}
            </a>
        {% endif %}
    {% else %}
        <a class="button" href="{{ path('homepage') }}">{{ 'backToYourReports' | trans({}, 'common') }}</a>
    {% endif %}

    <a class="link-icon link-icon-pdf button-link" href="{{ path('odr_pdf', {'odrId': odr.id}) }}">{{ 'downloadAsPdf' | trans() }}</a>

    <script>
        $(document).ready(function() {
            var ga = new opg.Ga({'timeout': 250});
            ga.trackDownloadableLink($('.link-icon-pdf'));
        });
    </script>
{% endblock %}
