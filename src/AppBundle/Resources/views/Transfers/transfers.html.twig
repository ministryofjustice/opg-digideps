{% embed 'AppBundle:Components/Page:page-report-full.html.twig' %}

    {% set translationDomain = "report-transfers" %}
    {% trans_default_domain translationDomain %}

    {% block pageTitle %}{{ 'page.htmlTitle' | trans }}{% endblock %}
    {% block contentClass %}{{ parent() }} js-report-contacts-input{% endblock %}
    {% block title %}{{ 'page.pageSectionTitle' | trans }}{% endblock %}

    {% block pageBody %}

        <p class="page-section-description}}">
            {{ 'page.pageSectionDescription'|trans({'%client%': report.client.firstname}, translationDomain ) }}
        </p>

        {% if report.contacts %}
            {% include 'AppBundle:Components/Page:_page-section-description.html.twig' with {
            transDomain: translationDomain,
            pageSectionDescription: 'page.pageSectionEditInfo'
            } %}
        {% endif %}

        <div id="transfers"/>Loading...</div>
        <script>
            $(document).ready(function () {
                new GOVUK.StickyHeader('#page-section-title-wrapper');
            });

            var opg = opg || {};
            opg.accounts = {{ report.accounts | serialize | raw }};

        </script>
        <script src="/javascripts/transfers.min.js"></script>

    {% endblock %}

    {% block pageBodyFooterNav %}
        {% include 'AppBundle:Components:nextprevious.html.twig' with {
        'previousLink': path("accounts_balance" , {'reportId': report.id }),
        'previousLabel': 'Balance accounts',
        'nextLink': path("assets" , {'reportId': report.id }),
        'nextLabel': 'Assets'
        } %}
    {% endblock %}
{% endembed %}
