{% extends 'AppBundle:Layouts:application.html.twig' %}
{% set transOptions = {'%client%': report.client.firstname | e } %}

{% import 'AppBundle:Macros:macros.html.twig' as macros %}

{% set translationDomain = "report-prof-deputy-management-costs" %}
{% trans_default_domain translationDomain %}
{% set page = 'managementCosts' %}

{% block htmlTitle %}{{ (page ~ '.htmlTitle') | trans }}{% endblock %}
{% block pageTitle %}{{ (page ~ '.pageTitle') | trans }}{% endblock %}
{% block pageTitleClass %}heading-large{% endblock %}

{% block breadcrumbs %}{{ macros.breadcrumbs(report) }}{% endblock %}

{% block linkBack %}
    {{ macros.linkBackStep(backLink, 'back' | trans({}, 'common')) }}
{% endblock %}

{% block pageContent %}

    <p class="heading-small push-half--bottom  text">
        {{ (page ~ '.pageSectionDescription') | trans(transOptions) }}
    </p>

    {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

    {% for profDeputyManagementCost in form.profDeputyManagementCosts %}
        <div class="{{ profDeputyManagementCost.moreDetails is defined ? ' js-details-expander ' : '' }}">
            {{ form_row(profDeputyManagementCost.profDeputyManagementCostTypeId) }}
            {{ form_input(profDeputyManagementCost.amount, 'amount', {
                'inputPrefix' : '£',
                'inputClass' : 'form-control-1-4 js-format-currency',
                'labelText': (page ~ '.form.entries.' ~ profDeputyManagementCost.profDeputyManagementCostTypeId.vars.value ~ '.label') | trans,
                'labelRaw': true
            }) }}

            {# this is testing the form field is defined. Not the entity property #}
            {% if profDeputyManagementCost.moreDetails is defined %}
                <div class="">
                    {{ form_input(profDeputyManagementCost.moreDetails, 'moreDetails', {
                        'labelText': (page ~ '.form.entries.' ~ profDeputyManagementCost.profDeputyManagementCostTypeId.vars.value ~ '.moreInformation') | trans,
                        'formGroupClass': 'panel panel-border-narrow',
                        'labelRaw': true
                    }) }}
                </div>
            {% endif %}
        </div>
    {% endfor %}

    {{ macros.saveAndContinueButton(form.save) }}

    {{ form_end(form) }}

{% endblock %}
