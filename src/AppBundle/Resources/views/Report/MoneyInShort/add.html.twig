{% extends 'AppBundle:Layouts:application_new.html.twig' %}

{% import 'AppBundle:Macros:macros.html.twig' as macros %}

{% set translationDomain = "report-money-short" %}
{% trans_default_domain translationDomain %}
{% set transOptions = {'%client%': report.client.firstname} %}

{% block htmlTitle %}{{ 'addPage.htmlTitle' | trans }}{% endblock %}
{% block pageTitle %}{{ 'addPage.pageSectionTitle' | trans }}{% endblock %}
{% block pageTitleClass %}heading-large{% endblock %}

{% block supportTitleTop %}
    <span class="heading-secondary">{{ 'addPage.supportTitle' | trans }}</span>
{% endblock %}

{% block linkBack %}
    {{ macros.linkBackStep(backLink, 'addPage.backLink' | trans({}, 'common')) }}
{% endblock %}

{% block pageContent %}

    <p class="heading-small">{{ 'addPage.pageSectionDescription1' | trans(transOptions) }}</p>
    <p class="text">{{ 'addPage.pageSectionDescription2' | trans(transOptions) }}</p>
    <p class="text">{{ 'addPage.pageSectionDescription3' | trans(transOptions) }}</p>

    {% include 'AppBundle:Report/MoneyInShort:_form.html.twig' with {
    'translationDomain': translationDomain,
    'form': form
    } %}

{% endblock %}
