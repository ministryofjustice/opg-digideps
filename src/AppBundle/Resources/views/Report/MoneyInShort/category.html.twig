{% extends 'AppBundle:Layouts:application_new.html.twig' %}

{% import 'AppBundle:Macros:macros.html.twig' as macros %}

{% set translationDomain = "report-money-short" %}
{% trans_default_domain translationDomain %}
{% set transOptions = {'%client%': report.client.firstname} %}

{% block htmlTitle %}{{ ('categoryPage.moneyIn.htmlTitle') | trans }}{% endblock %}
{% block pageTitle %}{{ ('categoryPage.moneyIn.pageSectionTitle') | trans }}{% endblock %}
{% block pageTitleClass %}heading-large{% endblock %}

{% block linkBack %}
    {{ macros.linkBackStep(backLink, 'stepPage.backLink' | trans) }}
{% endblock %}

{% block pageContent %}

    {{ form_start(form, {attr: {novalidate: 'novalidate' }}) }}

    <p class="text">
        {{ 'form.moneyShortCategoriesIn.label' | trans(transOptions) }}
    </p>
    <p class="form-hint">
        {{ 'form.moneyShortCategoriesIn.hint' | trans(transOptions) }}
    </p>
    {% include 'AppBundle:Report/MoneyInShort:_checkboxes_list.html.twig' with {
    'categoryKey': 'form.moneyShortCategoriesIn',
    'elements': form.moneyShortCategoriesIn,
    } %}

    {{ macros.saveAndContinueButton(form.save) }}

    {% if skipLink %}
        <a href="{{ skipLink }}"
           class="button-link behat-link-step-skip">{{ 'stepPage.skipLink' | trans({}, 'common') }}</a>
    {% endif %}

    {{ form_end(form) }}

{% endblock %}

