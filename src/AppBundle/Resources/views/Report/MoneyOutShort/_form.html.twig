{{ form_start(form, {attr: {novalidate: 'novalidate'} }) }}

    {{ form_input(form.description, 'form.transactionsOut.description', {
        'formGroupClass': 'explanation',
    }) }}
    {{ form_input(form.amount, 'form.transactionsOut.amount', {
        'inputPrefix' : '£',
        'inputClass' : 'form-control-1-4 js-more-info js-format-currency',
        'formGroupClass': ' form-group-value'
    }) }}

    {{ form_known_date(form.date, 'form.transactionsOut.date') }}

    {{ macros.saveAndContinueButton(form.save) }}

{{ form_end(form) }}
