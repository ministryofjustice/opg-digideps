{# Layout to extend for decisions subpages (decisioon CRUD + mental capacity so far) #}
{% extends 'AppBundle:Layouts:report_left_nav.html.twig' %}

{% block pageTitle %}{{ 'page.htmlTitle' | trans({}, translationDomain) }}{% endblock %}
{% block bodyClasses %}{{ parent() }} js-report-assets-input{% endblock %}
{% block title %}{{ 'page.pageSectionTitle' | trans({'%client%': report.client.firstname}, translationDomain) }}{% endblock %}

{% block backLinktop %}{% endblock %}

{% block leftcontent %}
    {% set route = app.request.attributes.get('_route') %}
    <nav id="account-sub-nav">
        <a href="{{ path('assets', {'reportId': report.id}) }}" class="{{ ('asset' in route) ? 'active':'' }} behat-link-decisions">{{ 'navLink' | trans({}, 'report-assets') }}</a>
    </nav>
{% endblock %}
