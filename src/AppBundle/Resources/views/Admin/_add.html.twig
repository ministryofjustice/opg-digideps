{% trans_default_domain "admin" %}

{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

  {% include 'AppBundle:Components/Form:_input.html.twig' with {
    element: form.email,
    labelClass: 'form-label-bold',
    labelText: 'user.email.label' | trans,
    hintText: 'user.email.hint' | trans
  } %}

  {% include 'AppBundle:Components/Form:_input.html.twig' with {
    element: form.firstname,
    labelClass: 'form-label-bold',
    labelText: 'user.firstname.label' | trans,
    hintText: 'user.firstname.hint' | trans
  } %}

  {% include 'AppBundle:Components/Form:_input.html.twig' with {
    element: form.lastname,
    labelClass: 'form-label-bold',
    hintText: 'user.lastname.hint' | trans,
    labelText: 'user.lastname.label' | trans
  } %}

  {% include 'AppBundle:Components/Form:_button.html.twig' with {
  element: form.save,
  labelText: 'user.submit.label' | trans
} %}


{# KNOWN DATE #}
{# {% include 'AppBundle:Components/Form:_known-date.html.twig' %} #}


{{ form_end(form) }}
