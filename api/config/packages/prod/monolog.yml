monolog:
    handlers:
        main:
            type: stream
            path: php://stderr
            level: "%log_level%"
            formatter: logstash_formatter
            bubble: false
            channels: ["!csv"]
        csv:
            type: stream
            path: php://stdout
            level: "%csv_log_level%"
            formatter: logstash_formatter
            bubble: false
            channels: ["csv"]
