monolog:
    channels: ["verbose"]
    handlers:
        main:
            type: stream
            path: "%log_path%"
            level: warning
            formatter: line_formatter
            channels: ["!verbose"]
        csv:
            type: stream
            path: "%log_path%"
            level: notice
            formatter: logstash_formatter
            channels: ["verbose"]
