codecov:
  comment:
    layout: "reach, diff, flags, files"
    behavior: default
    require_changes: false  # if true: only post the comment if coverage changes
  status:
    project:
      default:
        threshold: 5%  # if PR drops coverage by less than % percent, then successful.
