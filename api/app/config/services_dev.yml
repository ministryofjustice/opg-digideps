services:
    _defaults:
        autowire: true
        autoconfigure: true

        bind:
            $fixtureParams: "%fixtures%"
            $fixturesEnabled: "%env(bool:FIXTURES_ENABLED)%"

    App\Tests\Behat\:
        resource: "../tests/Behat/bootstrap/*"
