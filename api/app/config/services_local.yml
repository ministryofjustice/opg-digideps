services:
    _defaults:
        autowire: true
        autoconfigure: true

        bind:
            $fixtureParams: "%fixtures%"
            $symfonyEnvironment: "%kernel.environment%"

    App\Tests\Behat\:
        resource: "../tests/Behat/bootstrap/*"

    App\Tests\Behat\v2\Helpers\FixtureHelper:
        arguments:
            $fixturesEnabled: "%env(bool:FIXTURES_ENABLED)%"
