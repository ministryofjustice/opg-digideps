# Doctrine Configuration
doctrine:
    dbal:
        connections:
            default:
                driver: pdo_pgsql
                host: "%database_host%"
                port: "%database_port%"
                dbname: "%database_name%"
                user: "%database_user%"
                password: "%database_password%"
                sslmode: "%database_ssl_mode%"
                sslrootcert: "%kernel.project_dir%/certs/eu-west-1-bundle.pem"
                server_version: 14.18
                wrapper_class: OPG\Digideps\Backend\DBAL\ConnectionWrapper
                charset: UTF8
            ingestwriter:
                driver: pdo_pgsql
                host: "%database_host%"
                port: "%database_port%"
                dbname: "%database_name%"
                user: "%database_user%"
                password: "%database_password%"
                sslmode: "%database_ssl_mode%"
                sslrootcert: "%kernel.project_dir%/certs/eu-west-1-bundle.pem"
                server_version: 14.18
                wrapper_class: OPG\Digideps\Backend\DBAL\ConnectionWrapper
                charset: UTF8
        types:
            uuid: Ramsey\Uuid\Doctrine\UuidType

    orm:
        default_entity_manager: default
        auto_generate_proxy_classes: "%kernel.debug%"
        proxy_dir: "%kernel.cache_dir%/doctrine/orm/Proxies"
        entity_managers:
            default:
                connection: default
                # disable ORM annotation mapping
                auto_mapping: false
                mappings:
                    commonEntities:
                        type: attribute
                        dir: "%kernel.project_dir%/src/Entity"
                        prefix: OPG\Digideps\Backend\Entity
                        is_bundle: false
                filters:
                    softdeleteable:
                        class: Gedmo\SoftDeleteable\Filter\SoftDeleteableFilter
                        enabled: true
            # separate entity manager for use during ingest, to prevent transactions interfering with queries
            ingestwriter:
                connection: ingestwriter
                # disable ORM annotation mapping
                auto_mapping: false
                mappings:
                    ingestEntities:
                        type: attribute
                        dir: "%kernel.project_dir%/src/Entity"
                        prefix: OPG\Digideps\Backend\Entity
                        is_bundle: false
                filters:
                    softdeleteable:
                        class: Gedmo\SoftDeleteable\Filter\SoftDeleteableFilter
                        enabled: true
