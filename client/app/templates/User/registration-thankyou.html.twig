{% extends '@App/Layouts/application.html.twig' %}

{% trans_default_domain "register" %}

{% block htmlTitle %}{{ 'htmlTitle' | trans }}{% endblock %}
{% block pageTitle %}{{ 'pageTitle' | trans }}{% endblock %}
{% set transOptions = {
    '%email%': email,
    '%link%': path('login')
} %}

{% block pageContent %}

    <h2 class="govuk-heading-m flush--top">{{ 'thankyou.subheading' | trans }}</h2>

    <div>
        <p class="govuk-body">
            {{ 'thankyou.pageDescription1' | trans }}
            <strong class="bold-small">{{ email }}</strong>
            {{ 'thankyou.pageDescription2' | trans }}
        </p>
        <p class="govuk-body">{{ 'thankyou.pageDescription3' | trans }}</p>
        <p class="govuk-body">
            {{ 'thankyou.pageDescription4' | trans }}
        </p>
        <p class="govuk-body">
            <a href="{{ path('login') }}">Sign in</a>.
        </p>
    </div>

{% endblock %}
