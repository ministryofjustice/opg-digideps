{% props h="1" %}{# 1 / 2 / 3 / 4 / 5 / 6 #}
{% set h_tag = "h" ~ h %}

<{{ h_tag }} {{ attributes.defaults({class: 'govuk-summary-card__title'}) }}">
    {% block content %}{% endblock %}
</{{ h_tag }}>
