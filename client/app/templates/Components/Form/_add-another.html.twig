{# variables to be interpolated into translations #}
{% set transVars = {'%thing%': thingTranslationKey | trans({}, translationDomain)} %}

<h3 class="govuk-heading-m">
    {{ 'addAnotherThing.heading' | trans(transVars, 'common') }}
</h3>

{% set checkboxGroupOpts = {
    'fieldSetClass': 'inline',
    'legend': {
        'isPageHeading': false,
        'caption': false,
        'text': 'addAnotherThing.label' | trans(transVars, 'common')
    },
    'legendClass': 'form-label',
    'useFormGroup': true,
    'element': addAnother,
    'translationDomain': translationDomain
} %}

{% include '@App/Components/Form/_checkboxgroup.html.twig' with checkboxGroupOpts only %}
