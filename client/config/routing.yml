app:
    resource: "../src/Controller/"
    type:     annotation

logout:
    path: /logout

# redirecting the admin home
root:
    path: /pa
    defaults:
        _controller: FrameworkBundle:Redirect:redirect
        route: org_dashboard
        permanent: true
