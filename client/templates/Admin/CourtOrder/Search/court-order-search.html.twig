{% extends '@App/Layouts/application.html.twig' %}

{% trans_default_domain "admin-clients" %}
{% set page = 'courtOrderSearch' %}

{% set navSection = 'clients' %}

{% block htmlTitle %}{{ (page ~ '.htmlTitle') | trans }}{% endblock %}
{% block pageTitle %}{{ (page ~ '.pageTitle') | trans }}{% endblock %}

{% block helpline %}{% endblock %}

{% block pageContent %}

    {{ form_start(form, {attr: {class: 'search', novalidate: 'novalidate' }}) }}

    {{ form_input(form.q, 'Search', {
        'labelText': (page ~ '.searchBy') | trans,
        'inputClass': '' })
    }}
    {{ form_submit(form.search, '', { 'labelText': 'Search' }) }}

    {{ form_end(form) }}

    <hr>

    {# LIST #}
    {% if app.request.get('search_clients') is not null %}

    <div class="court-order-list">

        <h2 id="court-order-list-title" class="govuk-heading-m flush--ends">{{ (page ~ '.courtOrderTable.heading') | trans }}</h2>
        {% if courtOrders | length  == filters.limit %}
            <p class="govuk-body">
                {{ (page ~ '.courtOrderTable.resultsLimited.line01') | trans }}<br>
                {{ (page ~ '.courtOrderTable.resultsLimited.line02') | trans }}
            </p>
        {% else %}
            <p>
                Found {{ courtOrders | length }} Court Orders
            </p>
        {% endif %}


        {# Court Order list #}
        <table class="width-two-thirds">
            <thead>
                <tr>
                    <th scope="col">{{ (page ~ '.courtOrderTable.caseNumber') | trans }}</th>
                    <th scope="col">{{ (page ~ '.courtOrderTable.clientSurname') | trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% if courtOrders | length %}
                    {% for courtOrder in courtOrders %}
                        {% set discharged = "" %}
                        <tr>
                            <td>
                                <a class="govuk-!-font-weight-bold" href="{{ path('admin_court_order_details', {'caseNumber' : courtOrder.casenumber}) }}">
                                    {{ courtOrder.casenumber }}
                                </a>
                            </td>
                            <td>
                                {{ courtOrder.clientsurname }}
                            </td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="2">{{ (page ~ '.courtOrderTable.noResults') | trans  }}</td>
                    </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
    {% endif %}

{% endblock %}
