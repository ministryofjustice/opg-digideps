name: "[Tests] Synchronise Lambda Unit Tests"

defaults:
  run:
    shell: bash

on:
  workflow_call:

jobs:
  synchronise-lambda-unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@96f53100ba2a5449eb71d2e6604bbcd94b9449b5 # pin@v3
      - name: run unit tests
        id: unit-tests
        run: go test
        working-directory: lambdas/functions/synchronisation
