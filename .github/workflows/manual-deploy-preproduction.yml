name: "[Manual] Deploy Specific Image to Preproduction"

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy"
        required: true
      task_name:
        description: "Task to deploy"
        required: false
        default: "none"

permissions:
  id-token: write
  contents: read
  security-events: none
  pull-requests: none
  actions: none
  checks: none
  deployments: none
  issues: none
  packages: none
  repository-projects: none
  statuses: none

jobs:
  terraform_apply_preproduction:
    name: preproduction environment apply terraform
    uses: ./.github/workflows/_run-terraform.yml
    needs:
      - latest_deployed_image
    with:
      workspace: ddls6781994
      terraform_path: environment
      apply: true
      account_name: development
      container_version: ${{ github.event.inputs.image_tag }}
      pull_request_path: true
    secrets:
      ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY_ALLOW_LIST_REPOSITORY }}

  run_task:
    name: run specified task
    uses: ./.github/workflows/_run-task.yml
    needs:
      - terraform_apply_development
    secrets:
      ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY_ALLOW_LIST_REPOSITORY }}
    with:
      workspace: ddls6781994
      account_id: 454262938596
      account_name: development
      task_name: "integration_test_v2"
      timeout: "1200"
      override: "sh,scripts/task_run_console_command.sh,digideps:api:process-lay-csv,--env=prod,--no-debug,layDeputyReport.csv"
